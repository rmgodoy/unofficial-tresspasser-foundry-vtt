<section class="{{cssClass}} trespasser-sheet item-sheet" autocomplete="off">
  <header class="sheet-header item-header-layout">
    <div class="header-column image-column">
      <img class="profile-img" src="{{item.img}}" data-edit="img" title="{{item.name}}"/>
    </div>

    <div class="header-column details-column">
      <h1 class="item-title">
        <input name="name" type="text" value="{{item.name}}" placeholder="{{localize 'TRESPASSER.Sheet.Header.Name'}}"/>
      </h1>
    </div>
  </header>

  <section class="sheet-body scrollable">

    <div class="field-item">
      <label>{{localize "TRESPASSER.Sheet.Item.Details.ItemType"}}</label>
      <select name="system.subType">
        {{selectOptions subTypeOptions selected=system.subType localize=true}}
      </select>
    </div>

    {{#if hasCustomAttributes}}
    <div class="custom-attributes-area">
      <!-- Contextual sections based on SubType -->
      <div class="attribute-fields">
        {{#if isResource}}
        <div class="attribute-group flex-row">
          <div class="field-item">
            <label>{{localize "TRESPASSER.Sheet.Item.Details.ResourceType"}}</label>
            <select name="system.resourceType">
              {{selectOptions resourceTypeOptions selected=system.resourceType localize=true}}
            </select>
          </div>
        </div>
        {{/if}}

        {{#if isLightSource}}
        <div class="attribute-group flex-row">
          <div class="field-item">
            <label>{{localize "TRESPASSER.Sheet.Common.Radius"}}</label>
            <input type="number" name="system.radius" value="{{system.radius}}"/>
          </div>
          
          <div class="field-item">
            <label>{{localize "TRESPASSER.Sheet.Item.Details.DepletionDie"}}</label>
            <select name="system.depletionDie">
              {{selectOptions config.depletionDieOptions selected=system.depletionDie localize=true}}
            </select>
          </div>

          <div class="field-item checkbox-item">
            <input type="checkbox" name="system.active" {{checked system.active}} id="item-active-{{item.id}}"/>
            <label for="item-active-{{item.id}}">{{localize "TRESPASSER.Sheet.Item.States.ActiveLit"}}</label>
          </div>

          <div class="field-item checkbox-item">
            <input type="checkbox" name="system.usesFuel" {{checked system.usesFuel}} id="item-fuel-{{item.id}}"/>
            <label for="item-fuel-{{item.id}}">{{localize "TRESPASSER.Sheet.Item.Details.UsesFuel"}}</label>
          </div>
        </div>
        {{/if}}



        {{#if isConsumable}}
        <div class="attribute-group flex-row">
          <div class="field-item">
            <label>{{localize "TRESPASSER.Sheet.Common.Tier"}}</label>
            <select name="system.tier">
              {{selectOptions tierOptions selected=system.tier localize=true}}
            </select>
          </div>
          
          {{#if hasDamage}}
          <div class="field-item" style="flex: 2;">
            <label>{{localize "TRESPASSER.Sheet.Common.Damage"}}</label>
            <input type="text" name="system.damage" value="{{system.damage}}"/>
          </div>
          {{/if}}
        </div>
        {{/if}}
      </div>

      <!-- Drag & Drop sections -->
      <div class="nested-items-container">
        {{#if (or isConsumable isArtifact)}}
          <div class="nested-item-wrapper">
            {{> "systems/trespasser/templates/item/parts/effects-list.hbs" 
                title=(localize "TRESPASSER.Sheet.Item.Sections.EnhancementEffects") 
                items=system.effects 
                property="effects" 
                deleteClass="remove-effect"}}
          </div>
        {{/if}}

        {{#if (or isScroll isArtifact)}}
          <div class="nested-item-wrapper">
            {{> "systems/trespasser/templates/item/parts/deeds-list.hbs" 
                title=(localize "TRESPASSER.Sheet.Combat.Deeds") 
                items=system.deeds 
                property="deeds" 
                deleteClass="remove-deed"}}
          </div>
        {{/if}}

        {{#if isEsoteric}}
          <div class="nested-item-wrapper">
            {{> "systems/trespasser/templates/item/parts/deeds-list.hbs" 
                title=(localize "TRESPASSER.Sheet.Item.Sections.Incantations") 
                items=system.incantations 
                property="incantations" 
                deleteClass="remove-incantation"
                placeholder="TRESPASSER.Sheet.Item.Placeholder.DropIncantations"}}
          </div>
        {{/if}}

        {{#if isArtifact}}
          <div class="nested-item-wrapper">
            {{> "systems/trespasser/templates/item/parts/deeds-list.hbs" 
                title=(localize "TRESPASSER.Sheet.Item.Sections.Talents") 
                items=system.talents 
                property="talents" 
                deleteClass="remove-talent"
                placeholder="TRESPASSER.Sheet.Item.Placeholder.DropTalents"}}
          </div>
          <div class="nested-item-wrapper">
            {{> "systems/trespasser/templates/item/parts/deeds-list.hbs" 
                title=(localize "TRESPASSER.Sheet.Item.Sections.MagicTraits") 
                items=system.features 
                property="features" 
                deleteClass="remove-feature"
                placeholder="TRESPASSER.Sheet.Item.Placeholder.DropFeatures"}}
          </div>
        {{/if}}
      </div>
    </div>
    {{/if}}

    <div class="field-item">
      <label>{{localize "TRESPASSER.Sheet.Common.Price"}}</label>
      <input type="number" name="system.price" value="{{system.price}}" data-dtype="Number" />
    </div>

    <div class="field-item">
      <label>{{localize "TRESPASSER.Sheet.Common.Slot"}}</label>
      <input type="number" name="system.slotOccupancy" value="{{system.slotOccupancy}}" data-dtype="Number" />
    </div>

    <div class="field-item flex-row" style="gap: 10px; margin-top: 5px; flex-wrap: wrap;">
      <div class="field-item checkbox-item">
        <input type="checkbox" name="system.isLightFuel" {{checked system.isLightFuel}} id="item-is-fuel-{{item.id}}"/>
        <label for="item-is-fuel-{{item.id}}">{{localize "TRESPASSER.Sheet.Item.Details.IsLightFuel"}}</label>
      </div>
      <div class="field-item checkbox-item">
        <input type="checkbox" name="system.isAmmo" {{checked system.isAmmo}} id="item-is-ammo-{{item.id}}"/>
        <label for="item-is-ammo-{{item.id}}">{{localize "TRESPASSER.Sheet.Item.Details.IsAmmo"}}</label>
      </div>

      {{#unless isLightSource}}
      <div class="field-item checkbox-item">
        <input type="checkbox" name="system.equippable" {{checked system.equippable}} id="item-equippable-{{item.id}}"/>
        <label for="item-equippable-{{item.id}}">{{localize "TRESPASSER.Sheet.Item.States.Equippable"}}</label>
      </div>
      {{/unless}}

      {{#if (or system.equippable isLightSource)}}
      <div class="field-item" style="flex: 1; min-width: 80px;">
        <select name="system.placement">
          {{selectOptions placementOptions selected=system.placement localize=true}}
        </select>
      </div>
      {{/if}}
    </div>

    <div class="field-item description-column">
      <label>{{localize "TRESPASSER.Sheet.Common.Description"}}</label>
      <div class="trp-editor-container">
        <prose-mirror name="system.description" value="{{system.description}}" toggled="false" compact="false" editable="{{editable}}">{{{descriptionHTML}}}</prose-mirror>
      </div>
    </div>
  </section>
</section>

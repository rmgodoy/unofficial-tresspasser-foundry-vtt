<div class="effects-section">
  <h3 style="font-family: var(--trp-font-header); font-size: var(--fs-13); color: var(--trp-gold-bright); margin-bottom: 5px;">{{title}}</h3>
  <div class="applied-effects-list drop-zone" data-type="{{property}}" style="min-height: 48px; padding: 4px; border: 1px dashed #4a3f2f; border-radius: 4px; background: rgba(0,0,0,0.15);">
    {{#each items as |effect index|}}
      {{> "systems/trespasser/templates/item/parts/effect-chip.hbs" effect=effect index=index deleteClass=../deleteClass editable=true editableIntensity=true }}
    {{else}}
    <span style="color: #666; font-style: italic; font-size: var(--fs-11); margin-left: 8px;">{{localize "TRESPASSER.Sheet.Items.DropPlaceholder.Effects"}}</span>
    {{/each}}
  </div>
</div>

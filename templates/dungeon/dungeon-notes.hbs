<section class="tab notes {{tab.cssClass}}" data-group="primary" data-tab="notes">
  <div class="dungeon-notes">
    <h2>{{localize "TRESPASSER.Dungeon.Notes"}}</h2>
    <prose-mirror name="system.notes" value="{{system.notes}}" toggled="false" compact="true">{{{enrichedNotes}}}</prose-mirror>
  </div>
</section>

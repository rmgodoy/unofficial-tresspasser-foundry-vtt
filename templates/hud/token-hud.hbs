<div class="trespasser-hud-wrapper">
    {{#if inCombat}}
    <div class="hud-content">
        <header>
            <div class="hud-title" title="{{actorName}}">{{actorName}}</div>
            <div class="ap-indicator" title="{{localize "TRESPASSER.Sheet.Combat.ActionPoints"}}">
                {{#if isGM}}
                <button class="gm-ap-btn" data-action="modify-ap" data-delta="-1" title="{{localize "TRESPASSER.HUD.Resource.RemoveAP"}}">-</button>
                {{/if}}
                {{#each apDots}}
                <div class="ap-icon {{#if active}}active{{/if}}" {{#if active}}data-action="spend-ap" role="button" title="{{localize "TRESPASSER.HUD.Resource.SpendAP"}}"{{else}}title="{{localize "TRESPASSER.Sheet.Combat.ActionPoints"}}"{{/if}}></div>
                {{/each}}
                {{#if isGM}}
                <button class="gm-ap-btn" data-action="modify-ap" data-delta="1" title="{{localize "TRESPASSER.HUD.Resource.AddAP"}}">+</button>
                {{/if}}
            </div>
        </header>

        <div class="hud-actions">
            <!-- ATTEMPT DEED -->
            <div class="hud-action-group">
                <button class="hud-action-btn attempt-deed" data-action="toggle-panel" data-panel="attempt-deed" {{#unless canAttemptDeed}}disabled{{/unless}}>
                    <span class="btn-text">{{localize "TRESPASSER.HUD.Action.AttemptDeed"}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                        <div class="flexible-icon"></div>
                    </div>
                    {{#if (trespasserGt deedFocusCost 0)}}
                    <span class="focus-cost-chip">{{localize "TRESPASSER.HUD.Resource.FocusCost" count=deedFocusCost}}</span>
                    {{/if}}
                </button>
                <div class="hud-sub-panel hidden" id="panel-attempt-deed">
                    <div class="field-row">
                        <select name="attempt-deed-id">
                            {{#each deeds}}
                            <option value="{{id}}">[{{tierLabel}}] {{name}} ({{focusCost}}F)</option>
                            {{/each}}
                        </select>
                    </div>
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.HUD.Resource.APCost"}}</label>
                        <select name="attempt-deed-ap">
                            {{#each deedOptions}}
                            <option value="{{cost}}">{{label}}</option>
                            {{/each}}
                        </select>
                    </div>
                    <button class="action-confirm-btn" data-action="execute-attempt-deed">{{localize "TRESPASSER.HUD.Common.Confirm"}}</button>
                </div>
            </div>

            <!-- MANEUVER -->
            <div class="hud-action-group">
                <button class="hud-action-btn maneuver" data-action="toggle-panel" data-panel="maneuver" {{#unless canManeuver}}disabled{{/unless}}>
                    <span class="btn-text">{{localize "TRESPASSER.HUD.Action.Maneuver"}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                        <div class="flexible-icon"></div>
                    </div>
                    {{#if (trespasserGt maneuverFocusCost 0)}}
                    <span class="focus-cost-chip">{{localize "TRESPASSER.HUD.Resource.FocusCost" count=maneuverFocusCost}}</span>
                    {{/if}}
                </button>
                <div class="hud-sub-panel hidden" id="panel-maneuver">
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.HUD.Resource.APCost"}}</label>
                        <select name="maneuver-cost">
                            {{#each maneuverOptions}}
                            <option value="{{cost}}">{{localize "TRESPASSER.HUD.Option.Maneuver" cost=cost bonus=bonus}}</option>
                            {{/each}}
                        </select>
                    </div>
                    <button class="action-confirm-btn" data-action="execute-maneuver">{{localize "TRESPASSER.HUD.Confirm"}}</button>
                </div>
            </div>

            <!-- DEFEND -->
            <div class="hud-action-group">
                <button class="hud-action-btn defend" data-action="toggle-panel" data-panel="defend" {{#unless canDefend}}disabled{{/unless}}>
                    <span class="btn-text">{{localize "TRESPASSER.HUD.Action.Defend"}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                        <div class="flexible-icon"></div>
                    </div>
                </button>
                <div class="hud-sub-panel hidden" id="panel-defend">
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.Dialog.EffectEditor.TargetAttr"}}</label>
                        <select name="defend-type">
                            <option value="guard">{{localize "TRESPASSER.Sheet.Combat.Guard"}}</option>
                            <option value="resist">{{localize "TRESPASSER.Sheet.Combat.Resist"}}</option>
                        </select>
                    </div>
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.HUD.Resource.APCost"}}</label>
                        <select name="defend-cost">
                            <option value="1">{{localize "TRESPASSER.HUD.Option.DefendSingle"}}</option>
                            <option value="2" {{#if (trespasserGt 2 availableAP)}}disabled{{/if}}>{{localize "TRESPASSER.HUD.Option.DefendBoth"}}</option>
                        </select>
                    </div>
                    <button class="action-confirm-btn" data-action="execute-defend">{{localize "TRESPASSER.HUD.Confirm"}}</button>
                </div>
            </div>

            <!-- MOVE -->
            <div class="hud-action-group">
                <button class="hud-action-btn move" data-action="toggle-panel" data-panel="move" {{#unless canMove}}disabled{{/unless}}>
                    <span class="btn-text">{{moveBtnLabel}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                        <div class="flexible-icon"></div>
                    </div>
                </button>
                {{#if canUndo}}
                <button class="undo-btn" data-action="execute-undo" title="{{localize "TRESPASSER.HUD.Movement.Undo"}}">
                    <i class="fas fa-undo"></i>
                </button>
                {{/if}}
                <div class="hud-sub-panel hidden" id="panel-move">
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.Sheet.Combat.Speed"}}</label>
                        <span>{{localize "TRESPASSER.HUD.Movement.Value" value=speed}}</span>
                    </div>
                    {{#if moveActionTaken}}
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.HUD.Movement.Used"}}</label>
                        <span>{{movementUsed}} / {{movementAllowed}} {{localize "TRESPASSER.HUD.Movement.Label"}}</span>
                    </div>
                    {{else}}
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.HUD.Resource.APCost"}}</label>
                        <select name="move-cost">
                            {{#each moveOptions}}
                            <option value="{{cost}}">{{cost}} AP ({{localize "TRESPASSER.HUD.SpeedValue" value=dist}})</option>
                            {{/each}}
                        </select>
                    </div>
                    <button class="action-confirm-btn" data-action="execute-move">{{localize "TRESPASSER.HUD.Confirm"}}</button>
                    {{/if}}
                </div>
            </div>

            <!-- HELP -->
            <div class="hud-action-group">
                <button class="hud-action-btn help" data-action="toggle-panel" data-panel="help" {{#unless canHelp}}disabled{{/unless}}>
                    <span class="btn-text">{{localize "TRESPASSER.HUD.Action.Help"}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                        <div class="flexible-icon"></div>
                    </div>
                </button>
                <div class="hud-sub-panel hidden" id="panel-help">
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.HUD.Common.SelectAlly"}}</label>
                        <select name="help-target">
                            {{#each allies}}
                            <option value="{{id}}">{{name}} ({{distance}} sq)</option>
                            {{/each}}
                        </select>
                    </div>
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.HUD.Common.Attribute"}}</label>
                        <select name="help-attr">
                            <option value="accuracy">{{localize "TRESPASSER.Sheet.Combat.Accuracy"}}</option>
                            <option value="guard">{{localize "TRESPASSER.Sheet.Combat.Guard"}}</option>
                            <option value="resist">{{localize "TRESPASSER.Sheet.Combat.Resist"}}</option>
                            <option value="prevail">{{localize "TRESPASSER.Sheet.Combat.Prevail"}}</option>
                            <option value="tenacity">{{localize "TRESPASSER.Sheet.Combat.Tenacity"}}</option>
                        </select>
                    </div>
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.HUD.Resource.APCost"}}</label>
                        <select name="help-cost">
                            <option value="1">1 {{localize "TRESPASSER.Terms.Combat.AP"}} (+1)</option>
                            <option value="2" {{#if (trespasserGt 2 availableAP)}}disabled{{/if}}>2 {{localize "TRESPASSER.Terms.Combat.AP"}} (+2)</option>
                            <option value="3" {{#if (trespasserGt 3 availableAP)}}disabled{{/if}}>3 {{localize "TRESPASSER.Terms.Combat.AP"}} (+3)</option>
                        </select>
                    </div>
                    <button class="action-confirm-btn" data-action="execute-help">{{localize "TRESPASSER.HUD.Confirm"}}</button>
                </div>
            </div>

            <!-- PREVAIL -->
            <div class="hud-action-group">
                <button class="hud-action-btn prevail" data-action="toggle-panel" data-panel="prevail" {{#unless canPrevail}}disabled{{/unless}}>
                    <span class="btn-text">{{localize "TRESPASSER.Sheet.Combat.Prevail"}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                        <div class="flexible-icon"></div>
                    </div>
                </button>
                <div class="hud-sub-panel hidden" id="panel-prevail">
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.Sheet.Combat.States"}}</label>
                        <select name="prevail-state">
                            {{#each states}}
                            <option value="{{this.id}}">{{this.name}} ({{this.intensity}})</option>
                            {{/each}}
                        </select>
                    </div>
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.HUD.Resource.APCost"}}</label>
                        <select name="prevail-extra-ap">
                            <option value="0">1 {{localize "TRESPASSER.Terms.Combat.AP"}} (+0)</option>
                            <option value="1" {{#if (trespasserGt 2 availableAP)}}disabled{{/if}}>2 {{localize "TRESPASSER.Terms.Combat.AP"}} (+2)</option>
                            <option value="2" {{#if (trespasserGt 3 availableAP)}}disabled{{/if}}>3 {{localize "TRESPASSER.Terms.Combat.AP"}} (+4)</option>
                        </select>
                    </div>
                    <button class="action-confirm-btn" data-action="execute-prevail">{{localize "TRESPASSER.HUD.Confirm"}}</button>
                </div>
            </div>

            <!-- USE CONCOCTION -->
            <div class="hud-action-group">
                <button class="hud-action-btn concoction" data-action="toggle-panel" data-panel="concoction" {{#unless canUseConcoction}}disabled{{/unless}}>
                    <span class="btn-text">{{localize "TRESPASSER.HUD.Action.UseConcoction"}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                    </div>
                </button>
                <div class="hud-sub-panel hidden" id="panel-concoction">
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.Dialog.Concoction.Instruction"}}</label>
                        <select name="concoction-id">
                            {{#each concoctions}}
                            <option value="{{this.id}}">{{this.name}}</option>
                            {{/each}}
                        </select>
                    </div>
                    <button class="action-confirm-btn" data-action="execute-use-concoction">{{localize "TRESPASSER.HUD.Confirm"}}</button>
                </div>
            </div>
            <!-- TAKE AIM -->
            <div class="hud-action-group">
                <button class="hud-action-btn take-aim" data-action="toggle-panel" data-panel="take-aim" {{#unless canTakeAim}}disabled{{/unless}}>
                    <span class="btn-text">{{localize "TRESPASSER.HUD.Action.TakeAim"}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                        <div class="flexible-icon"></div>
                    </div>
                </button>
                <div class="hud-sub-panel hidden" id="panel-take-aim">
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.HUD.Resource.APCost"}}</label>
                        <select name="take-aim-cost">
                            {{#each takeAimOptions}}
                            <option value="{{cost}}">{{localize "TRESPASSER.HUD.Option.TakeAim" cost=cost bonus=bonus}}</option>
                            {{/each}}
                        </select>
                    </div>
                    <button class="action-confirm-btn" data-action="execute-take-aim">{{localize "TRESPASSER.HUD.Confirm"}}</button>
                </div>
            </div>
            <!-- INTERACT -->
            <div class="hud-action-group">
                <button class="hud-action-btn interact" data-action="toggle-panel" data-panel="interact" {{#unless canInteract}}disabled{{/unless}}>
                    <span class="btn-text">{{localize "TRESPASSER.HUD.Action.Interact"}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                        <div class="flexible-icon"></div>
                    </div>
                </button>
                <div class="hud-sub-panel hidden" id="panel-interact">
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.HUD.Resource.APCost"}}</label>
                        <select name="interact-cost">
                            {{#each interactOptions}}
                            <option value="{{cost}}">{{localize "TRESPASSER.HUD.Option.Interact" cost=cost bonus=bonus}}</option>
                            {{/each}}
                        </select>
                    </div>
                    <button class="action-confirm-btn" data-action="execute-interact">{{localize "TRESPASSER.HUD.Confirm"}}</button>
                </div>
            </div>
            <!-- SMASH -->
            <div class="hud-action-group">
                <button class="hud-action-btn smash" data-action="toggle-panel" data-panel="smash" {{#unless canSmash}}disabled{{/unless}}>
                    <span class="btn-text">{{localize "TRESPASSER.HUD.Action.Smash"}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                        <div class="flexible-icon"></div>
                    </div>
                </button>
                <div class="hud-sub-panel hidden" id="panel-smash">
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.HUD.Resource.APCost"}}</label>
                        <select name="smash-cost">
                            {{#each smashOptions}}
                            <option value="{{cost}}">{{localize "TRESPASSER.HUD.Option.Smash" cost=cost bonus=bonus}}</option>
                            {{/each}}
                        </select>
                    </div>
                    <button class="action-confirm-btn" data-action="execute-smash">{{localize "TRESPASSER.HUD.Confirm"}}</button>
                </div>
            </div>
            <!-- RUMMAGE -->
            <div class="hud-action-group">
                <button class="hud-action-btn rummage" data-action="execute-rummage" {{#unless canRummage}}disabled{{/unless}}>
                    <span class="btn-text">{{localize "TRESPASSER.HUD.Action.Rummage"}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                    </div>
                </button>
            </div>
            <!-- THROW -->
            <div class="hud-action-group">
                <button class="hud-action-btn throw" data-action="toggle-panel" data-panel="throw" {{#unless canThrow}}disabled{{/unless}}>
                    <span class="btn-text">{{localize "TRESPASSER.HUD.Action.Throw"}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                        <div class="flexible-icon"></div>
                    </div>
                </button>
                <div class="hud-sub-panel hidden" id="panel-throw">
                    <div class="field-row">
                        <label>{{localize "TRESPASSER.HUD.Resource.APCost"}}</label>
                        <select name="throw-cost">
                            {{#each throwOptions}}
                            <option value="{{cost}}">{{localize "TRESPASSER.HUD.Option.Throw" cost=cost range=range}}</option>
                            {{/each}}
                        </select>
                    </div>
                    <button class="action-confirm-btn" data-action="execute-throw">{{localize "TRESPASSER.HUD.Confirm"}}</button>
                </div>
            </div>
            <!-- VAULT -->
            <div class="hud-action-group">
                <button class="hud-action-btn vault" data-action="execute-vault" {{#unless canVault}}disabled{{/unless}} title="{{localize "TRESPASSER.HUD.Option.VaultTip"}}">
                    <span class="btn-text">{{localize "TRESPASSER.HUD.Action.Vault"}} ({{vaultRange}} {{localize "TRESPASSER.HUD.Movement.Label"}})</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                    </div>
                </button>
            </div>
            <!-- WAIT -->
            <div class="hud-action-group">
                <button class="hud-action-btn wait" data-action="execute-wait" {{#unless canWait}}disabled{{/unless}} title="{{localize "TRESPASSER.HUD.Option.WaitTip"}}">
                    <span class="btn-text">{{localize "TRESPASSER.HUD.Action.Wait"}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                    </div>
                </button>
            </div>
            <!-- FORCE MOVE (GM ONLY) -->
            {{#if canForceMove}}
            <div class="hud-action-group">
                <button class="hud-action-btn force-move" data-action="toggle-panel" data-panel="force-move" style="border-color: #e74c3c; color: #e74c3c;">
                    <span class="btn-text"><i class="fas fa-meteor"></i> {{localize "TRESPASSER.HUD.Action.ForceMove"}}</span>
                    <div class="btn-icons">
                        <div class="action-icon"></div>
                    </div>
                </button>
                <div class="hud-sub-panel hidden" id="panel-force-move" style="border-color: #e74c3c;">
                    <div class="field-row">
                        <label>Type</label>
                        <select name="force-move-type">
                            <option value="push">{{localize "TRESPASSER.HUD.ForcedMovement.Types.push"}}</option>
                            <option value="pull">{{localize "TRESPASSER.HUD.ForcedMovement.Types.pull"}}</option>
                            <option value="sweep">{{localize "TRESPASSER.HUD.ForcedMovement.Types.sweep"}}</option>
                            <option value="shove">{{localize "TRESPASSER.HUD.ForcedMovement.Types.shove"}}</option>
                            <option value="drag">{{localize "TRESPASSER.HUD.ForcedMovement.Types.drag"}}</option>
                        </select>
                    </div>
                    <div class="field-row">
                        <label>Distance</label>
                        <input type="number" name="force-move-distance" value="1" min="1" max="10" style="width: 40px; text-align: center; background: rgba(0,0,0,0.5); color: white; border: 1px solid var(--trp-border);" />
                    </div>
                    <button class="action-confirm-btn" data-action="execute-force-move" style="background: rgba(231, 76, 60, 0.2); border-color: #c0392b; color: #ff7675;">{{localize "TRESPASSER.HUD.Confirm"}}</button>
                </div>
            </div>
            {{/if}}
        </div>
    </div>
    {{/if}}
</div>
